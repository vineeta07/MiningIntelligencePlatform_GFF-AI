{
  "name": "blast-safety-checker-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx concurrently \"python -m uvicorn main:app --app-dir backend --host 127.0.0.1 --port 8000\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
